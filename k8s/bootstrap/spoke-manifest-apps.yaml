apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: spoke-manifest-apps
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - git:
        repoURL: ssh://git@ssh.github.com:443/homelab-user/homelab-k8s.git
        revision: HEAD
        files:
          - path: k8s/apps/*/overlays/spoke/manifest.yaml
  template:
    metadata:
      name: '{{appName}}-spoke'
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: '{{syncWave}}'
    spec:
      project: default
      source:
        repoURL: ssh://git@ssh.github.com:443/homelab-user/homelab-k8s.git
        targetRevision: HEAD
        path: 'k8s/apps/{{path[2]}}/overlays/spoke'
      destination:
        server: https://10.100.30.10:6443
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: hub-multisource-apps
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=default"]
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - git:
        repoURL: ssh://git@ssh.github.com:443/homelab-user/homelab-k8s.git
        revision: HEAD
        files:
          - path: k8s/apps/*/overlays/hub/multisource.yaml
  template:
    metadata:
      name: '{{.appName}}-hub'
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: '{{.syncWave}}'
    spec:
      project: default
      sources:
        - repoURL: '{{.repoURL}}'
          chart: '{{.chart}}'
          targetRevision: '{{.version}}'
          helm:
            releaseName: '{{.appName}}'
            valueFiles:
              - '$values/{{.valuesFile}}'
        - repoURL: ssh://git@ssh.github.com:443/homelab-user/homelab-k8s.git
          targetRevision: HEAD
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{.namespace}}'
      ignoreDifferences:
        - group: admissionregistration.k8s.io
          kind: MutatingWebhookConfiguration
          name: vault-agent-injector-cfg
          jqPathExpressions:
            - .metadata.labels
            - .metadata.annotations
            - .webhooks
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          jqPathExpressions:
            - .metadata.labels
            - .status
            - .spec.conversion
            - .spec.preserveUnknownFields
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
  templatePatch: |
    {{- if .serverSideApply }}
    spec:
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
          - ServerSideApply=true
    {{- end }}

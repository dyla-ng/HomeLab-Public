apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-auth
  namespace: cert-manager
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vault-auth
  namespace: cert-manager
spec:
  vaultConnectionRef: vso/vault-hub
  method: jwt
  mount: jwt-spoke
  jwt:
    role: vso-cert-manager-role
    serviceAccount: vault-auth
    tokenExpirationSeconds: 3600
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
  - name: hub-gateway
    namespace: gateway-system
  hostnames:
  - argocd.mgmt.lab.example.com
  rules:
  - backendRefs:
    - name: argocd-server
      port: 80
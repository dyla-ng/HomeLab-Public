apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-gateway-extauth
  namespace: authentik
spec:
  from:
  - group: gateway.envoyproxy.io
    kind: SecurityPolicy
    namespace: gateway-system
  - group: gateway.envoyproxy.io
    kind: SecurityPolicy
    namespace: authentik

  to:
  - group: ""
    kind: Service
    name: authentik-server
---

